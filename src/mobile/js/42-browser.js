// ============================================================
// In-app browser pane
// ============================================================
// The state machine that owns #screen-browser — openBrowser(), navigation,
// the docked/full-screen split, the /api/browse token — lands with the
// pane's behaviour, not this shell.
