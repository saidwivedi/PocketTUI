.ftp_credentials
.venv/
mobile_build/
__pycache__/
*.log
.backend_url
cloudflare_build/
.cloudflare_token
.cloudflare_token_email
.resend_api_key
.dev.vars
.turnstile_secret
node_modules/
.wrangler/
assets/landing_assets*/
# Composed from the landing screenshots by make_og_image.py.
assets/og-image.png
assets/landing.html
assets/favicon.ico
assets/favicon-32.png
_redirects
deploy_cloudflare.sh
.token
# Notification state: the VAPID keypair minted on first need, and the phones'
# push subscriptions. Both are this install's secrets, like the token.
.vapid.json
.push_subs.json
deploy_test.sh
# Built by setup_voice.sh: a ~150 MB model and a compiled binary.
voice/
.whisper-build/
# Opt-in voice debug captures (see resolver.py).
.last_voice.*
# Corrections learned from edited transcripts (see resolver.py). Personal
# vocabulary, local to this machine.
.voice_learned.json
explore/
# Assembled by build_mobile.py from src/mobile/. The runtime layout is flat, so
# these live at the root even though their source is split under src/.
/mobile_app.html
/sw.js
/icon-192.png
/icon-512.png
# Local working notes for this checkout, not part of the shipped project.
/CLAUDE.md
